import React from 'react';
import PropTypes from 'prop-types';


const NavBar = () => (
  <div>
    NavBar Component
  </div>
);

NavBar.propTypes = {};

NavBar.defaultProps = {};

export default NavBar;
