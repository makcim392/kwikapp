import React from 'react';

const ProductDetail = ({ product }) => (
  <div>
    ProductDetail
    <p>
      Product:
      {' '}
      {product.id}
    </p>

  </div>

);

export default ProductDetail;
